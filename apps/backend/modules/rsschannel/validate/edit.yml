fillin:
  enabled:                    true

fields:
  rss_channel{title}:
    required:
      msg:                    Es necesario ingresar el titulo del canal RSS
    sfPropelUniqueValidator:
      class:                  RssChannel
      column:                 title
      unique_error:           El nombre del canal ya existe
  rss_channel{link}:
    required:
      msg:                    El necesario ingresar el link del canar RSS